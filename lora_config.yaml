# LoRA hyperparameters for Bob fine-tuning
fine_tune_type: lora
optimizer: adamw
iters: 100
steps_per_eval: 20
learning_rate: 2e-5
batch_size: 4
num_layers: 24
lora_parameters:
  rank: 8
  dropout: 0.15
  scale: 10.0
