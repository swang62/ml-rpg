# LoRA hyperparameters for Bob fine-tuning
fine_tune_type: lora
optimizer: adamw
iters: 150
steps_per_eval: 50
learning_rate: 2e-5
batch_size: 4
num_layers: 28
lora_parameters:
  rank: 8
  dropout: 0.1
  scale: 8.0
